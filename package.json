{
  "name": "evercrest-fuel-management",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fuel dispatch: order, approve, load, deliver.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "node test/util.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
